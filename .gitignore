
# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli

# Expo / EAS workspace state — never commit, EAS regenerates on build
.expo/
.expo-shared/
expo_log.txt

# Native dirs are generated from app.json by `expo prebuild`
ios/
android/

# Dependencies
node_modules/

# Build outputs
dist/
web-build/

# Editor / OS
.vscode/
.idea/
.claude/
.DS_Store
*.log

# Transient build logs and diagnostic outputs
android_setup_log*.txt
diag_output.txt
git_status_output.txt

# Prebuild test scripts (iOS prebuild on Windows is impossible — see DEV_LEARNINGS.md)
prebuild_*.bat

# Dead-end Android setup attempts (project is iOS-only — see CLAUDE.md / DEV_LEARNINGS.md)
setup_android*.ps1
setup_android_emulator.bat
start_emulator.bat

# Local one-off helpers with hardcoded paths / per-session state
install_skills.bat
git_status_check.bat
